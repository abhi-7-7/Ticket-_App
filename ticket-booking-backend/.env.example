# Backend environment example
# Copy to `.env` and fill real values for development/production

# Server
PORT=4000
NODE_ENV=development

# MongoDB connection (placeholder URI)
MONGO_URI=mongodb://localhost:27017/ticket_booking_dev

# Session secret for express-session / Passport.js
SESSION_SECRET=your_session_secret_here
