# Frontend environment example
# Copy to `.env.local` (or use host env) and replace placeholder values

# Base URL for backend API (used by client to call API endpoints)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000/api
